# STW_TRANSFER_RERUN_V2

Owner explicitly authorized rerunning the full controlled STW experiment and publishing all code/results/weights to a separate GitHub branch. Previous local STW_TRANSFER_V1 artifacts are unavailable; previous numbers are historical only, not current results.

Status: RECOVERING_INPUTS; no new training results yet.

Starting commit: cce6af0a0756902f626a8832c3bdc25efa665b95.
Branch: research/stw-transfer-rerun-v2.

Preserve exact C_REBASE_V1 966 TRAIN rows / 24 people / six person-disjoint folds and comparator predictions. Restore the already qualified official STW release (14,278 full-face images; source groups and original splits; skin-only files are linked derived views). Use one ImageNet MobileNetV3-small architecture, ordinal MST pretraining B and identical pretraining C plus repeated-capture embedding consistency. Compare both frozen-head and limited-tail MSKCC transfer modes, with one same-architecture ImageNet-only control D. Register all numeric settings and source/code/manifest hashes before fitting. No DAST, CHROMA-FIT, ENCoDE or new dataset searches; no paid infrastructure; no MSKCC source-validation/calibration/test tuning.

Publish code/configuration before training; publish each completed checkpoint and receipt incrementally. Never publish participant photographs, masks, image caches, original identifiers, credentials or unaccepted data agreements. Native MSKCC instrument Lab remains distinct from human apparent MST. No intrinsic-reflectance or selfie instrument claim follows from MST or consistency.

Next: restore source files and runtime, verify masks and exact row mapping, freeze recipe, execute training and OOF, replay one fold, report all candidates including negative results. Production instrument estimator remains unchanged unless a preregistered candidate passes the comparison rule.
