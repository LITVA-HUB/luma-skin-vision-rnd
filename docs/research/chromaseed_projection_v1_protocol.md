# ChromaSeed-X: fit-only covariance projections for compact color prediction

Registered before new model fitting, 2026-09-13. Previous A turn is progress: read-only verifier returned passed with unchanged receipt SHA256 91a348711bbe95dba917a6b31c34e5c78d94c6113737a33f0a93ba4fcc5ec646. A progress is terminal, no train/audit/runtime worker remains. The two Python processes seen during the startup parallel scan were the verifier launched by this continuation, which exited0. Preserve all A/G/GS and older sources/results/receipts. Full compact/fast/high-quality goal active; local work only, no delegation/publication/downloads/new data.

## Question and data

Can an original-fit-only covariance projection reduce storage and improve predictive distances, without removing useful color information? D's acquisition separation and A/GS sensitivity motivate this question but do not establish covariance as a cause. This is actual new learning, not a claim of algorithmic novelty or phone validation.

Only original TRAIN train.npz, SHA256 d7e7b4b4fc4574d620cbac8364dd8bb91be51769f4045bb8b4ddf2ad840556e0; load color/target/patient/site/device. Keep frozen mixed/SLR→iPod/iPod→SLR roles and three person-disjoint camera-stratified inner folds. These 966 rows/24 people and roles were historically reused and overlap. No legacy validation/calibration/test, raw images or observed query Lab. Camera changes also change people/distribution.

## Projection and inference arithmetic

Start from the unchanged original-fit unweighted FP32 x/y means and standard deviations (population, floor1e-6). Input normalization executes in FP32 then promotes to FP64. Person/site/image-balanced fit weights have mean1. Compute their weighted mean m and covariance C of normalized original fit rows only. Use population denominator sum(w), symmetric C and descending eigenvalues clipped at0; orient each eigenvector so its largest-absolute component is positive. No target, query, augmentation or camera label enters this covariance.

Thirteen representations: raw identity; dimensions d∈{8,16,36} crossed with covariance shrinkage τ∈{0,.1,.5,1}. In each retained direction j the scale is [(1−τ)λj+τ trace(C)/36]^(−1/2), floored inside the square root at1e-4·max(trace(C)/36,1e-12). Projection is the leading eigenvectors times these scales. Store m and projection as FP32. For fit and queries, q=FP32[(z−mFP32)·PFP32], then promote q to FP64 for kernel arithmetic. Saved centers are those exact FP32 q rows. Identity bypasses both projection and new arrays; no round trip.

The scalar kernel width is the exact lower median of positive (>1e-10) pair RMS distances in that representation, floor1e-6, stored FP32. Recompute fit-only width and landmarks for each representation. Kernel exp(−||q−c||²/(2d·width²)), at most128 RPCholesky centers, seeds17/29/43. Use a variable-dimension column operator with the frozen streaming landmark selector. Raw uses the frozen A/P basis helper exactly; all raw payloads at eta0 must equal their corresponding frozen A bank arrays. Full36/τ1 is a scaled rotation control, expected to preserve distances approximately through FP32 arithmetic, not a claimed bit-identical payload.

Four learned families as in A: norm_static, norm_joint_soft, perceptual_static, perceptual_joint_soft. Projected kernels feed either Z or [Z,s(x)Z]. The continuous gate remains trained on original unprojected normalized color36 by the frozen G procedure and is shared across representations. Both coefficient blocks are learned jointly; ridge α∈{.1,1,10}; shared original-fit perceptual metric or I. No augmentation in X. A single-camera joint fit exactly aliases the same representation/alpha static model. No unknown-camera detector or query-time adaptation is introduced.

All final arrays FP32 except the existing uint8 soft flag. Projected models add mean36 and projection36×d; raw keeps the existing seven/eleven fields. If128 centers are retained, projected static bytes are7,244/12,492/25,612 for d8/16/36; active joint adds1,689B. Actual center count/payload/cache and response costs must be measured. A projected full36 model is larger than raw. Numerical compression alone does not prove useful quality.

## Counts, controls and ordering

Each bank:13 representations×4 families×3 seeds×3 alphas=468 configurations; six exact imported G soft controls (two losses×three seeds), six exact imported A guarded joint controls and one weighted native-Lab constant, total481. G/A control settings are their previously frozen role selections, matched to the same fit/query rows; imported controls are not refits.

Twelve banks total5,772 stored readouts: nine inner4,329 then three final1,443. Of5,616 new configurations,1,872 are single-camera aliases. Expected3,744 new coefficient solutions/624 shared Gram eigendecompositions;468 basis preparations (36 raw and432 projected),156 exact width calculations,12 covariance eigendecompositions and4 original-feature gate fits. Frozen raw basis helpers also compute36 baseline+36 theta auxiliary solves.144 imported G/A controls and12 constant fits are separate. All432 raw identity configurations must exactly equal A's eta0 controls, including their aliases. Record actual ranks and numerical solve diagnostics.

Inner scoring uses original queries only, no target-preserving synthetic transformations. Retain every candidate prediction and mean person ΔE00 plus image p90, then average errors over seeds. For each family and role there are39 representation/alpha score pairs. Two policies share this fixed grid:

- quality: minimum inner person error, ties smaller maximum numeric payload across inner folds/seeds, then larger alpha, then registered representation order.
- compact: among candidates within .05 of the best raw candidate's person error AND .10 of that same raw candidate's p90, minimize the maximum inner numeric bytes. Ties person error, p90, larger alpha, representation order. Choose the raw anchor by person error, p90, larger alpha. That anchor is always feasible. These inner allowances are not bounds on outer error or final payload.

Freeze all24 family/policy/role choices after all nine inner banks and before any final fit/evaluation. Retain all468 candidate score pairs and15 fixed reference score groups. Complete all three final banks before scoring72 selected new instances plus36 fixed imported controls and3 constants=111 records. Evaluate every final record with all33 frozen GS affine transformations; identity gives clean errors, four doses give worst-of-eight error/drift and person/camera summaries. No further dimension/shrinkage/policy choice after outer results. Existing A clean/guarded, G soft/hard, static P, full KRR, guided RBF and iterative R context remains in reporting.

Expected inner stored prediction rows: (734+323+643)×481=817,700. Final rows:111 model records spread over the three query roles×33=1,462,758. Seeds are basis repeats, not new people or an ensemble. Constants are not triplicated as seeds.

## Tests, independent audit and runtime

Before freezing/fitting: test weighted covariance/projector against SVD, expected whitening geometry/floor/scale, full rotation distance consistency, generic width against exhaustive pairs, streaming columns against dense kernels, raw identity exact A parity, single-camera aliases, coefficient solutions against independent augmented QR/SVD, compact-policy guards/ties, and actual NumPy-only consumer parity including serialization/invalid payload checks. Use toy data only until freeze; all selected final models must also pass actual consumer checks.

Audit all bank payloads/rows/hashes/controls/aliases, all OOF predictions/39-candidate policies and all final33-transform metrics. Independently verify covariance via a weighted-design SVD and reconstruction, fit normalizers, source-only weights, latent fit-center membership/indices and exact width. Direct variable-dimension kernels and frozen selector's dense counterpart check projected landmarks with documented numerical tolerance; independently refit all72 selected instances plus12 fixed active d8/τ.1/α.1 mixed fold0 probes (four families×three seeds) using a separately fitted gate, analytic metric and augmented-design QR/SVD. Require selected/probe native-Lab prediction drift≤.001, direct saved-payload predictions≤2e-8. No primary weights overwritten by audit. Eigenvalue gaps, clamping and ranks are descriptive, not generalization certificates.

After audit and no concurrent heavy workers, time actual batch-one standalone inference for111 final records,20 warmups/3 passes and exact original-output comparison. Profile24 selected settings at seed17, each warmup+3 full fits=96; four fixed mixed positive d8/τ.1/α.1 family probes add16. All112 complete fit outputs must match saved arrays exactly. Full fit includes normalization/covariance/projection/distances/landmarks/gate/readout; report all costs, excludes imports/I/O/search and image preparation. Cached arrays exclude process/temporary/retained caller objects. Standalone consumer must need NumPy only, no training import at inference.

Final report includes adverse transfer/stress outcomes, both policies, all candidates, constant controls, full dimensionality overhead and complete provenance verification. No automatic promotion from smaller size or historical outer improvement. Full user goal remains active unless ordinary-phone quality and integration are actually established.
