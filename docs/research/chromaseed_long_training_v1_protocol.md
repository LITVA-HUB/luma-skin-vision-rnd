# ChromaSeed-LT: more synthetic examples and longer continuation of the643-parameter head

2026-09-13, registered before implementation/fits. User explicitly requests more examples and long training of the light model. NB is sealed negative equivalence evidence; accepted NP blind-head weights are retained. No new measured people/images, datasets/weights/packages, agents, publication or legacy held validation/calibration/test. Use original TRAIN966rows/24people only, with existing overlapping exploratory roles and three person-disjoint inner folds. Ordinary-phone faces remain unvalidated.

Warm start: the643-parameter36->16ReLU->3 NP blind head, chosen original block2/3/2 for mixed/forward/reverse by the frozen NP inner policy. For each inner fold use ONLY the matching original ND inner blind4 model at its unchanged selected ND rate/checkpoint and correct seed slot. Never initialize an inner fit from a full-role model. For final fits use exact NP selected exports. Preserve original fit-only FP32 normalizers and NP payload/consumer format (2,886 numeric B,5,456 cached B). Reset AdamW moments for this new continuation; no claim to resume inaccessible original optimizer state. Step0 is the exact old predictor.

Earlier palette transfer and A's16 affine copies already exist. LT varies the number of sampled digital variants and the duration of continuation, not a novel augmentation principle. For every original fit row materialize256 bounded encoded-RGB affine contractions, plus original variant0. Seed each original global row independently with880003+1009*row_index. Draw256x4 float64 uniforms; first3 are RGB anchor, fourth sets dose in[0,4/255). Quantiles and mean transform v'=(1-t)v+t*anchor; std scales1-t; correlations unchanged. Keep original native Lab target by synthetic assumption, not a new instrument measurement. The first16 variants are a strict prefix of the same256 pool. All variants remain in their source person's fit partition, and normalizers use only original fit rows. Validate pool legal bounds and hash actual data.

Three modes:0,16,256 extra variants. In augmented modes each sampled source row is original with probability0.5; otherwise choose uniformly among its mode's variants. Mode0 always uses original. Source rows follow unchanged equal-person/site/image weights. Thus source mass is unchanged; adding variants does not multiply a person's weight. Shared source/variant uniforms across modes and rates isolate treatments. At131072 updates with minibatch64 each model receives8,388,608 presentations, about half digitally augmented in modes16/256; these are not millions of new people or measurements. Full mixed pool has734x257=188,638 feature/label variants.

Grid: seeds17/29/43, modes0/16/256, base rates0.0001/0.0003. Fixed18-slot order seed then mode then rate. Training uses FP32, TF32 off, deterministic CUDA; per-model MSE mean64x3 summed over models, AdamW betas.9/.999,eps1e-8,decay.01,per-model clip5. Learning-rate factor0.1+0.45*(1+cos(pi*(t-1)/(131072-1))), t starts1. Always retain ORIGINAL horizon131072 in shorter replay; never reschedule to selected steps. Checkpoints0/512/2048/8192/32768/131072. Pool generation and row/uniform sample prefixes must agree between full and shorter runs.

Nine inner banks(three roles xthree folds),18 trajectories each=162 continuation trajectories; each stores6 checkpoints including baseline,972 model records. For each role/mode score baseline once plus2rates x5nonzero checkpoints=11 candidates; total99 candidates. Mean of three seed equal-person DeltaE00 is primary, then mean seed image p90, fewer steps, lower rate. Choose one candidate per role/mode=9 choices. Also choose one overall candidate per role from all33 by(error,p90,steps,mode,rate)=3 choices. Baseline ties prefer mode0. Freeze all12 choices after inner results and before final-role fitting/evaluation. Retain baseline even if long training worsens results; do not pick an externally favorable checkpoint.

Three final banks at the same18-slot shape/order to131072,54 trajectories and324 checkpoint records. Evaluate every final checkpoint under all33 existing affine transforms; full curves are exploratory, selected choices fixed. Baseline0 is represented6 times per seed only for array/shape consistency; report9 unique full-role baseline weights, not54 independent initial models. Preserve exact FG references and NP blind baseline values for context. Total12 banks,216 actual continuation trajectories,1296 serialized checkpoint records including216 baseline aliases;1080 nonzero checkpoints. No bank size/slot dropping optimization in this study: NB showed shape-dependent gradient differences that can grow with long training.

Before freeze: tests for pool prefix/label/source mass, feature consistency, step0 identity, fixed-horizon rate prefixes, sampler prefixes, CPU/GPU train/export parity and fixed18-slot graph reset/replay. Independent audit checks warm-start lineage and fit/query disjointness, raw pool hashes with separate affine construction, all inner scores/choices, final predictions/metrics and actual single-vector consumers. Expected final prediction rows324/3 x1198x33=4,269,672. New NP-format outputs checked against independent neuron reductions, absolute native Lab tolerance2e-8. CUDA-vs-NumPy model arithmetic tolerance0.002, never a generalization guarantee.

Timing after audit: retain fixed18-slot bank and original horizon. For each role reconstruct the three NP warm starts with original ND singleton fits, verify weights against accepted NP, then run LT to the maximum nonzero selected step among its policies, saving all selected checkpoints. Repeat3 times; first is timing warmup only. Charge ALL three upstream ND fits, full pool/samples, all18 trajectories through required steps, graph setup/reset and export; do not divide bank time or call it one-head training cost. If every choice is step0, reconstruct the original NP baseline only and report no LT training was selected. Verify exact selected exports under the same bank shape; keep failures explicit. Actual final one-vector CPU timing includes NP normalization, excludes skin/face extraction. Full131072 research cost is reported separately from selected-recipe construction cost.

All sources/protocols/consumed parent artifacts frozen before selection. Full primary, audit and runtime receipts sealed afterward; report verifier read-only thereafter. Broad goal stays active until real end-to-end facial quality is established.
