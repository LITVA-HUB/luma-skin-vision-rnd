# ChromaSeed-WA: same-trajectory checkpoint means at unchanged compact size

2026-09-13. Registered before new weight-mean exports or scoring. Previous goal turn is progress: LT completed216 long trajectories, selected and audited the results; its receipt18b9127d82ca32e854be7efbf3906dd4f1fa73a749bf9d41ab7a78eae7df795b was freshly verified read-only. Full high-quality facial goal remains active. Current original-TRAIN966rows/24people, exact existing roles/inner folds only. No additional data/images/weights/packages, agents, publication, messages or legacy validation/calibration/test arrays.

Inventory: scoped rg across scripts/src/tests and research protocols/decisions found cc_v4_next_decision.md proposing an EMA teacher and cc_v5_protocol.md explicitly excluding EMA in that experiment. Earlier skin_color_sampling_protocol_v1.md and skin_sampling_transfer_next_decision.md already cover balancing/importance sampling. This is a scoped inventory, not proof that averaging never appeared anywhere. The SWA authors study averaging along SGD trajectories and changes in learning-rate schedule [Izmailov et al.,2018](https://arxiv.org/abs/1803.05407). WA here is a bounded arithmetic experiment on saved AdamW/cosine LT checkpoints, not a reproduction of their training recipe or reported generalization gains. No third-party implementation is acquired.

Question: can equal averaging of SAME-seed, SAME-original-head, SAME-augmentation/rate trajectory checkpoints reduce held-person color error while keeping643 parameters/2886 numeric bytes and one consumer pass? It does not implement dynamic inference or prove the wider goal by itself. Prior iterative/dynamic R/ND/NP attempts remain separate evidence. Weight means are not an ensemble of predictions; a negative result closes this specific postprocessing hypothesis without changing LT weights or selectors.

Parents: exact LT inner banks for matching role/fold and exact LT final banks for final construction. Metadata family/original_k/prefix, normalizers and36->16->3 shapes/dtypes must match. Enforce common context/seed/variants/rate provenance and distinct ascending checkpoint steps. Do not average different seeds or heads, permute neurons, fit interpolation coefficients, include query labels in means, change normalization or alter parent weights. Average only w0,b0,v0,c0, coordinatewise FP64 sum in step order divided by count then one cast to FP32. A singleton is a deep exact copy; all other metadata copied exactly. No new gradient training in primary WA.

Existing steps S=(0,512,2048,8192,32768,131072); modes0/16/256; rates.0001/.0003; seeds17/29/43 and fixed LT18-slot order. A canonical baseline at0 is one recipe per role/seed. For each of6 mode/rate arms use15 recipes: last at each of5 positive steps; adjacent pair at each of5 endpoints (first pair0,512); prefix means of all positive checkpoints through8192/32768/131072 (3 recipes); last-three-positive means ending32768/131072 (2 recipes). Duplicate component sets are removed by this predefined enumeration, not by observed performance. Thus91 recipes/role,273 three-seed candidate scores overall. Inner9banks x3seeds x91=2457 exports, including27 unique warm starts; across full role fit rows1700, total464100 OOF prediction vectors. Last31recipes/role correspond exactly to LT's33 candidates after collapsing its three baseline aliases.

For each role choose one recipe for each of last/pair/prefix/tail3, plus overall, total15 policy choices. Rank mean three-seed equal-person native D65/10-degree DeltaE00, then mean image p90, lower max contributing step, fewer checkpoints, lower variant count, lower rate, method order(last,pair,prefix,tail3). Each selected last choice MUST exactly match the old LT overall choice and score after baseline normalization. Freeze all15 choices before constructing/evaluating any new final-weight averages. All91 candidates retained; no outer-driven selector revision.

Final scope fixed by selection: export the union of chosen recipes, their corresponding last-checkpoint-at-the-same-mode/rate/endpoint controls, and canonical step0. De-duplicate identical recipe identifiers, keep all policy/control associations. Three seeds for every member. Expected final count is data-independent given the frozen selector; assert it against the union, never a hand-tuned subset. Evaluate every included model under all33 previously fixed affine transforms; same metric/person/camera limitations as LT. Verify singleton controls against exact LT saved predictions and preserve FG references as prior context. Never use the potentially favorable known final LT curves to pick means or settings.

Independent audit reconstructs every mean using coordinate-level scalar sums from bound parent arrays and authoritative slot lineage, verifies preserved metadata/shape, all2457 inner exports/464100 OOF vectors,273scores/15choices, all final predictions/metrics/transforms and real one-vector NP consumers. Tolerance2e-8 native Lab against separate neuron reductions; parent/singleton payloads and averaged scalar reconstruction must be array-exact. Report mean-weight prediction versus mean-prediction drift as an arithmetic diagnostic, never interchangeable. Synthetic tests include nonlinear counterexample, identity/nonmutation, wrong provenance/normalizer rejection, component enumeration and tie ordering.

Runtime after audit: all final one-vector consumer calls,20warmups/3passes, CPU one thread. For each role reconstruct three full original ND singleton warm starts at frozen prior NP settings, exact-export NP, then full fixed18-slot LT bank to maximum step required by selected recipes/endpoint controls, retaining every required contributing checkpoint plus0 and original131072 learning-rate horizon. Repeat3 times; first excluded only from timing. Verify all rebuilt parent checkpoint arrays against corresponding full LT banks before averaging and every selected/control export bitwise. Charge three original fits, entire pool/samples/bank/graph/export, average construction and validation. Never divide bank cost by18 or call historical saved-checkpoint averaging cost full training. Report pure averaging time separately; primary WA itself reuses historical weights. All full-LT historical costs remain available.

Seal primary sources before scoring, selector before final construction, then audit/runtime/report/card/decision. Existing LT/NP/ND/NB bound writers and G/GS mains must never rerun. Fresh read-only report verification after sealing. No goal completion absent validated ordinary-phone end-to-end quality.
