# Palette transfer P3: matched native initialization study

Previous goal turn: progress. HR verification consumers were implemented, tested against completed banks and frozen. HR primary remains live. This new study tests native transfer of the six verified P2 encoders; auxiliary TRAIN MSE and layer compatibility do not establish native color improvement.

## Intervention

Use patch5m, soft5m and dynamic5m (4,962,566 / 4,846,822 / 4,846,822 deployed parameters). For each architecture and native role, fix its head mode to the HR `per_architecture` choice made on its INNER data. This deterministic rule is registered before HR finishes; never choose a head from the external results. All three initialization arms for that architecture/role use exactly that same head. Reuse the corresponding sealed AS/HR original-initialization candidates and final records, after exact lineage checks.

Arms in stable order: original, aligned, shuffled. Original initializes exactly as HR. Aligned/shuffled replace only token1/token2's 105,856 parameters with the matching P2 seed/arm encoder. Compensate the first layer for the native FIT-only token mean/std using the already verified transplant formula. Other parameters, zero residual head, NP anchor, seed and parameter count remain unchanged. All residual weights, including the transferred encoder, are free to learn; the 643-parameter NP anchor stays frozen. No auxiliary head at inference. Export the initialization arm and encoder-content digest as nonnumeric provenance metadata; original exports remain bitwise identical to HR.

P2 provenance is fixed: 19 UMINHO TRAIN sources, 2,432 spatial patches, 19,456 photometric views, six encoders trained2048steps. Simulated views are not additional people or real camera measurements. Derived spectral Lab targets are not substituted for measured native targets. Retain the aligned/shuffled control and every negative native outcome. Any gain is conditional on the prior HR inner head selection; this does not exhaust possible head/initialization interactions.

## Native fit and selection

Only original MSKCC TRAIN:966observations/24people, color36/tokens64×18, native D65/10°Lab. Mixed, SLR→iPod and iPod→SLR roles with the existing person-disjoint three inner folds. They are historically reused/confounded exploratory roles, not fresh validation. No original validation/calibration/test, five private unreferenced portraits, LaPa mask examples or UMINHO held cubes in native fit/selection. Auxiliary source groups remain separate; no unverified cross-dataset identity assertion.

Preserve HR's six slots: seeds17/29/43 × rates1e-5/1e-4. AdamWwd0.01, clip5, batch64, FP32/deterministic, TF32off, cosine horizon8192. Record checkpoints128/512/2048 and stop2048. The matched budget allows exact reuse of original controls; do not claim this search exhausts the attainable benefit of longer transfer training. Same per-seed weighted draws across all arms. Four shared passes and dynamic connection masks retain HR semantics and dense computation costs.

New fits:54innerbanks+18finalbanks=72banks/432trajectories. Reused controls:27original innerbanks+9original finalbanks, plus NP/WE. Per role:3architectures×3arms×2rates×3steps+2controls=56candidates; total168. Choices:9per-pair+3per-architecture+1overall per role,39total. Stable ranking is (mean seed person ΔE00, mean seed image p90, numeric bytes, steps, lr or0), then original candidate order. Freeze every choice before any new final fit or external inference. There are54new final model records+27original architecture records+18NP/WE records=99. Overall yields one preselected architecture/arm (or control) and three seed outcomes per role; no post-hoc substitution by an external winner.

## Gates, costs and verification

Preparation can use CPU while HR runs. CPU tests must check exact original trajectories, transfer-normalization preservation, seed/arm alignment, unchanged nonencoder initialization, trainable encoder updates and source/export metadata. A separately recorded synthetic CPU preflight may use actual six P2 encoders and all9architecture/head combinations; it must not be reported as native fine-tuning or native accuracy. No GPU preflight/fit until HR's actual process is dead, actual tool session has terminal exit0 and HR audit/runtime/report are sealed.

Before production, the CUDA preflight must reproduce original HR exports bitwise for all9architecture/head combinations, reproduce short prefixes for aligned/shuffled, validate independent NumPy predictions/all passes at native Lab atol0.002/rtol1e-6, and respect peak6.5GB. Freeze production sources, exact encoder inputs, the completed HR seal and inherited dependencies before native fit. Failures remain visible; completed artifacts cannot be overwritten. Telemetry cannot abort training just because Windows temporarily holds its progress file; critical writes remain mandatory.

Independent full verification must reconstruct all54new inner banks,972checkpoint/slotmodels and183600OOFvectors; verify168candidates/39choices; inspect18new final banks/108slotpayloads and54selected exports/21564actual single-example calls, including every recurrent pass. Verify45inherited records and the head selection rule. Independently reproduce transplant parameters, warm lineage, fit-only normalizers, weighted draws and final metrics. Do not seal from preparation tests alone.

Measure81architecture/arm/seed responses afresh (NP/WE timings explicitly inherited),20warmups+3×64fixed query rows. Measure complete selected-bank construction for all27architecture/arm/role combinations with three upstream NP parents and all six continuation slots. Require bitwise models/predictions. Separate native fit cost, auxiliary P2 preparation/training cost, replays/audits and failure overhead. No dividing a six-slot bank timer by six, no presenting derived views as new independent examples, no phone-performance claim from a descriptor-only CPU timing.

Freeze the interpretation: aligned versus both original and shuffled is the principal contrast; preserve all arms, per-pass errors, ΔE00 and image fractions above5/10. This study tests initialization and fine-tuning, not proof of skin specificity versus all generic color pretraining. The broad quality goal remains active until an adequate native endpoint is verified.
