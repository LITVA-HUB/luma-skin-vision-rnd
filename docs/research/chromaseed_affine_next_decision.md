# After ChromaSeed-A

2026-09-13. This goal turn is **progress**, not completion or a repeated blocker. Previous GS state was revalidated: frozen source/input/artifact hashes, terminal primary/audit handles and no remaining worker. The full user goal remains a compact model that trains and responds quickly while predicting real skin color accurately. Compactness and CPU cost are measured; ordinary-phone facial generalization is still missing.

## What A changes

The joint soft model learns the base and correction together. Mixed perceptual clean error is 5.272642 versus G soft 5.295118 and static 5.390066, at 21,973 numeric bytes, 11.8 µs prepared-feature response and 34.600 ms full fit of 734 rows. This is a small exploratory result on six reused held people; joint inner error is worse than the controls and the descriptive paired interval includes zero. It is a candidate, not an independently selected overall winner.

Affine augmentation does not establish the user's palette-transfer hypothesis. All 12 clean choices retain eta=0. All 12 guarded choices use eta=.75; this slightly improves mixed/reverse synthetic stress, but harms SLR→iPod clean and stress error. Original target retention under digital corruption is only an assumption. No measured people or acquisition conditions were added. Stronger-ridge candidates and constant controls remain in the full grid; low output drift alone is not useful quality.

All 1,884 stored readouts, 144 imported G controls, 576 single-camera aliases, 2,402,100 inner query predictions and 24 policy choices were checked. Final 111×33 cases give 1,462,758 predictions. Independent QR/SVD refits cover 72 selected new instances plus 12 positive probes, maximum native-Lab difference 9.63e-6. All 112 actual timing fits reproduce arrays exactly. Primary PID35120, audit session18105, runtime and report are terminal exit0. No A job remains after final verification.

## Next bounded learning question — planned, not launched

Investigate whether the 36 strongly correlated color statistics make the kernel distance too sensitive to redundant acquisition cues. D found strong acquisition separation; GS and A show shared color sensitivity even with continuous gates. These observations motivate the question, but do not prove feature correlation is the cause.

Prepare a separately registered fit-only input projection study: original standardized 36 coordinates as the exact control, covariance-shrunk projections and fixed smaller dimensions such as 8/16 alongside full dimension. Estimate every mean/covariance/projection on original fit people only. Retain the same 128-center analytic readout, normalized/perceptual losses, seeds, ridge controls and fixed G/A controls. Include projection storage, computation and its effect on bandwidth/landmarks in the budget. A low-dimensional input could reduce center storage; no size or quality improvement has yet been measured.

Before fitting, freeze the complete dimension/shrinkage grid, metric scaling, person folds, tie rules and evidence limits. Separate unsupervised projection from target-conditioned directions; do not use query labels, query statistics, camera identities at inference or held-camera color support. Compare the identity projection numerically to the exact existing baseline before optimizing. Evaluate clean quality and all existing stress doses, so losing useful color information cannot count as robustness. Do not broaden eta or weak-ridge grids just to obtain a favorable outer result.

The next study is useful local progress and does not make the 24 historical people independent again. It cannot finish the full quality goal. Real product-facing evidence will require independently acquired, instrument-referenced ordinary-phone facial data and end-to-end feature extraction validation; no data acquisition is launched here. Preserve the exposed legacy validation/calibration/test exclusion, all old locks/results, negative outcomes, app/archive contents and lack of publication/delegation authorization.

[A report](../benchmarks/chromaseed_affine_v1/report.md) · [A verification](../benchmarks/chromaseed_affine_v1/verification.json) · [Active goal ledger](chromaseed_active_goal.md).
