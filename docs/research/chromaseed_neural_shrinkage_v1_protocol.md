# NS: one bounded stronger-penalty extension on exact NR bases

Registered 2026-09-13 before any stronger-alpha fit. NG independently checked756 designs/3780 df values and252 original inner scores.77/84 curves improve1→10; median equivalent-df reduction10→100 is10.906852, exceeding both registered criteria. NG receipt57b1d6b00b51e9f761ad34b2062f28a1186cd403f0ba40a52e24b0d105014e25. This motivates one extension, not indefinite grid growth. No role-specific choice uses outer performance. Original full compact/fast/high-quality goal stays active; NG is progress, not completion.

Original TRAIN only, SHA256d7e7b4b4fc4574d620cbac8364dd8bb91be51769f4045bb8b4ddf2ad840556e0. Same three reused roles, nine inner and three final banks. Import exactly504 NR bases (seven random/Adam1,4,16/Gaussian1,4,16 representations x two groups x three seeds x12 banks); hidden network, normalizers, target convention, rows and weights unchanged. No image/new data/package/weight acquisition, exposed legacy roles, agents or publication.

Alphas .1,1,10,100,1000; norm and local-perceptual objectives. Reuse the frozen NR fit_head and independent augmented-QR refit implementations. Fit all5040 heads, including3024 exact old-alpha controls and2016 stronger-alpha heads. Also preserve36 unchanged learned networks,6 FG controls and1 constant per bank. Total463 records per bank,5556 across12 banks. All3540 corresponding old NR models must be array-exact. No hidden training in the primary cached-basis sweep; this is disclosed in workflow time.

Each primary head is independently refitted by the existing pivoted-QR implementation using analytic CIEDE2000 tensors and independently checked person/site weights. Refit input rows must match the parent; no participant may cross that bank's fit/query boundary. Predictions must agree within.001 native Lab on every fit row and, for inner banks, every query row. Original controls also reproduce old OOF within1e-9. Retain both primary and QR payloads plus per-bank maxima for post-save checks.

Reassemble all787100 OOF rows (463x1700). Select84 per-basis alphas from420 candidate scores with original mean-person DeltaE00/p90/alpha-index ordering; then12 basis policies use original error/p90/epochs/basis-order. Choices are frozen before final-bank fits or query evaluation. Every original .1/1/10 candidate must reproduce NR's score within2e-8; reference scores also match. Final selection contains84 heads+43 references per role =381 payloads,252 new selected heads and129 exact old references. Evaluate all33 prior affine settings,5020818 prediction rows total. Report clean and all four prior stress doses. Selected old-alpha heads must retain exact payloads. Never choose a better outer head retrospectively.

After saving, independently check every selected actual one-row consumer against the primary stored predictions; independently score all clean/stress metrics. Compare primary and stored QR predictions for each selected new head over all33 transforms at.001 native Lab; reference payloads match NR. Also check every bank file hash and complete counts. Reports include every base/objective/group and every loss, not just improvements. Compare with matching old NR alpha-selected head and FG, not only with a weakened new baseline. Existing outer roles remain exploratory and people/camera composition is confounded.

Dedicated timing after quality audit: all381 actual consumers, previous warmup/three-pass procedure. For each84 selected head settings (seed17), reconstruct full model from raw fit arrays three times (warmup+two measured), including all representation epochs, normalization/initialization, head solve and export;252 full fits. Also time six FG role/group controls three times;18 fits. Total270 complete fits, with exact payload repeats counted and original2e-6 parameter/.001 Lab tolerances; nonexact tolerated repeats must be disclosed. No free cached representation in full fit timings. Deployment payload1855/10564 B remains unchanged; explicit transient arrays are not peak RAM. CPU1thread timings exclude image processing and are not RTX4060 or phone claims.

Freeze numerical sources and parent evidence before fitting. Keep primary/audit/timing/report receipts immutable after verification; plans and status remain mutable. An upper-boundary selection at1000 does not authorize another range expansion without a new substantive hypothesis. General model quality remains unproved without suitable independent phone/face evidence.
