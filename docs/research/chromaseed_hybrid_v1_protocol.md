# ChromaSeed H: shared-center raw/projected corrections

2026-09-13. Registered before any H real-data fit. Exploratory continuation of X, not an independent replication. The user authorizes autonomous compact-model experiments. No agents, publication, new data, or legacy validation/calibration/test access.

## Question and fixed construction

Does retaining the original color36 model while learning a projected correction improve the accuracy/size tradeoff that failed under X's projected-only camera transfer? Compare ordinary convex blending, uniform residual learning, and a continuous support-weighted residual with matched capacity. No topology-learning, confidence-calibration, unknown-camera-detection or true-error-bound claim.

Use only the SHA-bound original TRAIN, the three historical roles, original person-disjoint three inner folds, person/site/image weights, and seeds17/29/43. Normalizers, gate, covariance, metric, both widths and landmark selection see the fit rows only. There are no query labels, camera IDs, batch statistics, synthetic copies, or role-dependent switches in a predictor.

The raw backbone is exactly A's joint-soft eta0/alpha0.1 model for each of normalized and shared-perceptual losses. Its existing acquisition gate is fitted only when both acquisition groups occur; that absence removes the joint column block but **does not disable either H correction**. Original A control payloads must reproduce exactly. Base coefficients are frozen while fitting a residual; this is staged analytical learning, not a new backpropagation or iterative-reflection algorithm.

Use the fixed X projection d16/tau0.5. This choice comes from the already-seen X exploration and is explicitly not fresh evidence. Apply original FP32 normalization, then FP64 centered projection and FP32 rounding, then FP64 kernels. Weighted covariance and shrinkage floor follow X unchanged. Both Gaussian widths use the exact lower median of positive original-fit pairwise RMS distances and FP32 storage. Pair-distance storage remains quadratic.

Select up to128 landmark row indices with the original raw RPCholesky basis. The projected branch uses **these same row indices**; diagonal entries are1, symmetrized Kmm eigendecomposition retains eigenvalues above1e-8 of the maximum, and whitens the columns. A projected-only matched control is newly fitted on those shared rows. It is not called equivalent to X's separately selected projected landmarks. Import the exact X d16/tau0.5/alpha0.1 joint model as an additional fixed reference.

For active joint models each branch has design D=[Z,clip(gate,-1,1)Z]; otherwise D=Z. Learn the projected-only readout against normalized Lab. Learn residual readouts against normalized Lab minus the **exported FP32 raw model's actual prediction**. Minimize sum_i w_i (D_i theta-r_i)' M (D_i theta-r_i)+alpha||theta||², where M is I or the same original-target shared perceptual metric. Use the existing Gram/eigen analytic solver. Every positive residual fit is made independently of its deployment strength rho.

Raw-kernel coverage is c(x)=mean_j Kraw(x,center_j). Support is h(x)=c(x)^p, with p in{1,4}. It is a smooth, bounded kernel average with **no fitted threshold**; values are not calibrated probabilities or a guarantee of safety away from training. Fit support residuals using design h(x)D and deploy with the same h(x). No label-dependent coverage calibration or self-distance percentile. Uniform residual uses D.

Deployment equations in normalized Lab: blend=(1-rho)raw+rho projected; uniform=raw+rho residual; support=raw+rho h residual. Grid alpha in{0.1,1,10}, positive rho in{0.25,0.5,1}; support power in{1,4}. Every blend/uniform/support selection includes one exact zero-correction raw candidate. Blend rho1 is canonicalized to the projected-only payload. All fields needed to derive projected centers are stored; hybrid payload stores the raw centers once, projection, projected width/readout, mode, rho, power, and optional projected joint coefficients. Cached projected centers and both kernels count in runtime/memory. Projected-only payload uses the existing X schema. All36 input statistics remain required.

## Counts and selection

Per bank:6 raw controls,18 matched projected-only,54 blends,54 uniform residuals,108 support residuals =240 H records; plus6 imported X models and1 fitted constant =247. Twelve banks give2,964 stored records;216 rho1 blend aliases,72 raw A exact controls,72 imported X controls and12 constants must be reported separately. Zero controls are shared candidates, not extra stored fits.

Each bank computes3 raw and3 shared projected bases,2 widths,1 covariance, at most1 gate;3+3 frozen raw-helper auxiliary solves. Per seed:1 raw Gram with2 solutions;1 projected Gram with6;2 loss-specific residual Grams with3 each;4 loss/power-specific support Grams with3 each. Total26 readout solutions and8 Gram eigendecompositions/seed:936 new solutions,288 Gram decompositions,72 basis preparations,24 widths,12 covariances,4 gates,36+36 auxiliary solves across12 banks. Imported and strength-reused records are not counted as new fits.

Inner selection uses mean person ΔE00, first computed separately per seed then averaged (no ensemble). For each role and loss independently choose raw(1 candidate), projected(3), blend(10 including raw), uniform(10 including raw), support(19 including raw). Among candidates within1e-5 ΔE00 of the lowest score, prefer smaller numeric payload, then lower clean score, lower image p90, larger alpha, smaller rho, smaller power. The fixed1e-5 tie tolerance prevents interpreting last-bit changes as a useful improvement. It is not a significance or generalization bound. That makes258 candidate score records and30 selected settings. Report9 fixed reference groups. Freeze all30 choices before any final fitting/evaluation.

Finals:30 settings x3 seeds=90 selected models;18 fixed X references and3 constants =111 records. Evaluate all33 previously registered legal affine feature transforms (identity and8 RGB corners at1/255,4/255,16/255,64/255), with unchanged targets as a **synthetic assumption**. Preserve all per-person/camera and dose outcomes; no post hoc suppression of adverse transfer results. Total OOF color predictions419,900; final predictions1,462,758. Independent fixed-prediction paired person resampling is descriptive only; roles and people are historically reused, seeds are not people.

## Verification, runtime, interpretation

Before source freeze: numerical tests for shared centers, endpoint canonicalization, same-support training/deployment algebra, single-camera activity, solver agreement with QR, raw A parity, NumPy-only consumer and deterministic single-fit parity. Audit all12 banks, original data/sources, inner predictions/scores/choices, final summaries and predictions. Independently reconstruct the geometry with weighted SVD/exhaustive widths/dense pivoting, fit selected readouts by QR/SVD, and force positive blend/uniform/support probes even if a family selects rho0. Tolerances: independent output<=0.001 native Lab, stored-model consumer/direct prediction<=2e-8, normal equation relative residual<=1e-8. Investigate discrepancies without silently changing frozen training code or choices.

Measure actual NumPy one-record prediction and cached arrays for all111 selected/reference records. Time four complete fits (one warmup, three measured) for each of30 selected seed17 settings, plus six fixed mixed-role positive controls: two losses x blend/uniform/support, alpha0.1,rho0.5,power4 for support. Thus144 full timing fits, checking payloads exactly. Use one CPU thread without concurrent heavy jobs; report hardware and scope. Fit timing includes constructing both needed representations, gate/metric/widths/readouts and export, not data loading or imports. No GPU speedup claim. Keep paired same-run raw timings, including zero-correction aliases.

Create a reproducible report and immutable verification receipt, preserve older evidence and negative outcomes, update the active-goal ledger. Ordinary phone facial quality, face/skin extraction, lighting, cosmetics matching, commercial integration and Skolkovo eligibility remain unestablished by these24 historical people. Compactness alone cannot complete the broader goal.
