# Unmodified reference subset: Google FFCC

Fetched 2026-09-10 from https://github.com/google/ffcc at commit `2fa9e1316954dbd3913630b7d597927941b4dd32` using commit-pinned raw.githubusercontent.com URLs. GitHub commit/tree metadata supplied source blob IDs and byte sizes. `MANIFEST.json` records each fetched file's Git blob, size and SHA-256. 85 upstream files total 241,767 bytes; the complete directory including this provenance and manifest remains below 2 MB.

Included: LICENSE, README, Train.m, internal MATLAB helpers, DefaultHyperparams.m and two Gehler-Shi project constant files. These are unmodified reference files. They have not been run or integrated into executable project code. Their dataset-loading and model-export helpers are present only as source documentation.

Excluded: all data directories, images, labels, checkpoints, fitted project hyperparameter files, minFunc, PDFs and any third-party port. No pretrained weights or benchmark data were fetched.

Upstream code is Apache-2.0; preserve LICENSE and source copyright notices. Our manifest and provenance are additions. MATLAB and minFunc rights/dependencies are separate; no clearance for datasets or trained weights is inferred from the code license. See `../../cc_v3_prior_art.md` for the source audit, source/paper differences and limitations on a Python reproduction claim.
