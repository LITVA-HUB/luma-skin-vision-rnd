# ChromaSeed-P8: small local branch on the accepted compact color head

2026-09-13, registered before implementation and source-data fitting. Previous goal turn is verified progress: WA sealed negative averaging evidence; fresh read-only verificationf82f7f7d31b5639abbfef57ce96c79709022205b5d583291cf932cd823b1bdc0 passed. Broad compact/fast/high-quality facial goal remains active. No new acquisition, images, external weights/packages, agents, publication or messages. Only original TRAIN cache SHA256d7e7b4b4fc4574d620cbac8364dd8bb91be51769f4045bb8b4ddf2ad840556e0,966rows/24people; no legacy validation/calibration/test or raw image arrays.

Inventory: actual R code/summary and protocol inspected. R patch_mlp uses18->32->24 encoder, mean-pool, color36 plus24 into96->64->48->3 residual head and weighted ridge anchor;16731parameters/67828numericB/~42.3us, errors5.472118/11.214075/8.829750. Its recurrent/dynamic controls already exist and did not establish universal quality; do not rename those experiments as new. Original pixel code defines each64x18 token as RGB quantiles .1/.5/.9(9),mean3,std3,and .5*mean(abs horizontal difference)+.5*mean(abs vertical difference)(3). Histograms/copula context also already studied elsewhere. Those historical protocols use different models/roles and are not matched P8 baselines.

P8 compares two arms within ONE fixed18-slot bank: stats and patch8. Seeds17/29/43, arms in that order, rates.0001/.0003/.001 in that order. Original NP643 head36->16ReLU->3 warms each arm from exactly matching existing ND inner-fold models or NP final exports at unchanged original head2/3/2,rate/checkpoint. No final warm starts initialize inner fits. Preserve original fit-only x/y FP32 normalizers. Reset optimizer moments. Token normalizers: original FIT tokens mean/std over rows and64 patches, FP64 calculation castFP32, std floor1e-6. No camera identifier or grouping metadata enters predictions.

Added local branch: normalized token18 -> linear8+ReLU, independently for all64 tokens. Pool mean8, stabilized population spread8=sqrt(mean((h-mean)^2)+1e-6), max8, concatenate24. The spread epsilon prevents undefined zero-variance backward; it is not exact unregularized std. Add pooled24 @ g24x16 to the EXISTING color36 @ w0+b0 preactivation, then same ReLU16/output3. Initialize g=0; local weights u18x8 from CPU NumPy RNG(seed+770003), uniform+-1/sqrt18, local bias e8=0. Each rate/arm shares this initialization for the same seed. Keep separate color and local matrix products to preserve the NP function atstep0 and explicitly test it. No cross-seed averaging, distillation target or iterative inference in this study.

Total patch arm1179learned values: NP643 plus u144/e8/g384=536; expected5174numericB including color/token/target normalizers and two uint8 lineage scalars,9888cached-arrayB for NumPy FP64 weights and FP32 normalizers. Stats arm masks pooled24 to zero in training, so g remains exactly zero and local branch gradients are zero; its exported model discards u/e/g/token normalizers and is the NP643/2886numericB/5456cachedB format. Training still evaluates the fixed bank including inactive branch work; do not claim the stats fit is cheaper from bank timing. Both arms fit in a common1179-coordinate layout to keep batch shape/order constant. Initial patch payload stores unused local parameters even though its prediction equals NP; baseline aliases are not independent models.

Only original real descriptors train, no synthetic augmentation in P8. Equal-person/site/image weighted replacement, minibatch64. Share one source-index stream per seed across arms/rates (same seed+9001 RNG convention); preserve prefixes in shorter replay. MSE of normalized Lab mean64x3 per model, SUM18 model losses. AdamW betas.9/.999,eps1e-8,decay.01,per-model clip5. Cosine rate factor.1+.45*(1+cos(pi*(t-1)/(32768-1))), fixed ORIGINAL horizon32768 for shorter replay. Original immutable base rates must not alias mutable optimizer rates. FP32 deterministic CUDA/TF32 off. Whole-step CUDA Graph,3warmups, restore weights/zero moments/counter/rates before actual updates. Checkpoints0/512/2048/8192/32768; each trajectory receives2097152presentations at the full horizon. This schedule/bank differs from LT; only step0 is an exact old control, no unchanged-LT-trajectory claim.

Nine inner banks18trajectories each=162;810checkpoint records,153000OOF vectors across1700 reused full fit rows. For each role/arm score baseline once plus3rates x4positive steps=13candidates;78scores total. Choose per arm and overall,9policies, by(error,p90,numeric_bytes,steps,rate-or0). Mean three seed equal-person native D65/10-degree DeltaE00, not ensemble predictions. Freeze choices before three final banks and evaluation. Three final banks54trajectories,270records incl54baseline aliases. Whole primary216trajectories/1080records incl216baseline aliases;864positive checkpoints. Retain all positive and adverse curves; do not replace selector using external errors.

All270 final checkpoints evaluated under33 existing affine transforms: identity plus8RGB corners at1/255,4/255,16/255,64/255. Transform color36 by the existing moment rule. Jointly transform token first12 columns v'=(1-t)v+t*RGBanchor, repeated4times; last6 std/gradient columns scale1-t. Use FP64 transform then castFP32, no new physical target measurements. Synthetic tests independently extract tokens from generated continuous RGB and verify this affine law; do not load real pixel arrays. Frozen transformed target is an illumination-invariance assumption, not true measured GT after digital editing. Expected3,558,060final prediction vectors/consumer calls. New consumer one finite color36 plus64x18tokens; stats ignores optional tokens and uses exact NP consumer. Include actual64-token normalization/encoding/pooling in patch response timings, exclude image decoding/extraction and state that limitation.

Before freeze: behavioral tests of step0 identity/deep export, data-only fit normalization, parameter/byte counts, token permutation invariance, joint affine law, finite constant-token backward, zero stats g and nonzero learned patch g, CPU/GPU parity, deterministic fixed18-shape short replay and original-horizon/LR prefix invariants. Independent audit checks warm lineage/fit-query disjointness, original token normalizers, all810inner checkpoints/153000OOF vectors and78scores/9choices, all270final checkpoints/transforms/metrics and actual consumers. Direct independent neuron/patch reductions in FP64 after FP32 normalization, native Lab tolerance2e-8; CUDA/NumPy tolerance.002, no quality guarantee. Check input permutations and branch ablation without using them to select weights; record their effects only as diagnostics.

Runtime after audit:20warmups/3single-vector passes for every final export. Reconstruct original three ND singleton models and NP warm starts, then full fixed18-slot P8 bank through maximum selected step among the two arm/overall policies, original horizon32768 and all needed checkpoints. Repeat3 times, first timing warmup only. Verify original parents and every selected exported payload bitwise. Charge all upstream fits, token preparation, shared bank work/graph/export/validation, never divide bank time by18 or call full recipe single-model cost. Report research bank cost and model-only response separately. If both arms selectstep0, original construction alone suffices for stats; constructing patch baseline also charges token normalizers/local initialization/export. No isolated shortcut should change training replay shape.

All new sources/protocol/consumed parents frozen before scoring/fitting; selection frozen before final fit/evaluation. Seal audit/runtime/report/card/decision, reverify read-only afterward. Preserve all WA/LT/NP/ND/NB frozen writers and never G/GS mains. Completion still requires ordinary-phone facial/end-to-end evidence absent here; no claim that software checks finish the broad goal.
