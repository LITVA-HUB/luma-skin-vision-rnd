# After measured palette P1

Update 06:39 Moscow: auxiliary pretraining P2 is complete and independently audited. Six shared local encoders, seeds17/29/43 × aligned/shuffled targets, trained2048steps on19,456fixed views from2,432real spatial patches. Mean final TRAIN MSE0.17805aligned versus0.97916shuffled; not held/native accuracy. All6encoder exports transfer into patch5m/soft5m/dynamic5m local layers within1.67e-6. [P2 report](../benchmarks/chromaseed_palette_pretrain_v1/report.md). Native fine-tuning has not run. Before scheduling another GPU study, complete preparation of HR's full independent auditor and runtime/report consumers; the current full HR gate is still outstanding.

This continuation is progress. Exactly 19 original TRAIN UMINHO faces yielded 19,456 selected, measured spectra. Source readback and independent integration passed for every sample; all 19 masks were visually reviewed. See the [P1 report](../benchmarks/skin_spectral_palette_v1/report.md). The overall research goal remains active: no ordinary-phone native-color improvement has been demonstrated from this palette.

Use this as a candidate auxiliary pretraining source, with face-level weights and explicit derived-rendering conventions. Preserve the original 5 validation and 5 test cubes until the next protocol fixes training, selection and endpoints. Do not treat thousands of pixels or simulated illuminants as thousands of independent people/cameras. Do not combine the new palette with the live frozen HR comparison.

Before a transfer experiment, register matched conditions for native data only versus palette pretraining plus the same native fine-tuning. Fix architecture, initialization seeds, optimization budget, camera/participant split, model-selection rule and inference interface before any new held exposure. Separate reconstruction or simulated-light performance from measured native-color performance. Any CIE 1931/2° versus 1964/10° bridge needs an explicit target convention; no global relabeling of MSKCC/DAST/phone data.

Retain reflectance above 1: 43 selected spectra contain 143 such channels. A globally bounded logit-reflectance representation is not directly valid for this dataset. Any proposed transform, exclusion or tail extension must be defined and compared without tuning on held sources. Seg1's selected pixels are automated candidates, with uncalibrated confidence on derived spectral renderings and imperfect eye/hair boundaries; positive-only rectangles do not certify full-mask purity.

The immediate GPU priority is to complete and independently audit the registered HR series. The initial HR process failed on a Windows progress-file replacement at step 1664 of the nineteenth bank. An I/O-only recovery launcher preserves the 185 original source bindings and all 18 completed banks; see [recovery protocol](chromaseed_head_range_recovery_v1.md). Recover operational costs from both attempts, including 357.052568 seconds of discarded partial-bank training. A resumed job's own wall timer is incomplete.
