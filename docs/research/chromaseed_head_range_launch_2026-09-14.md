# ChromaSeed HR: серия запущена

Состояние на14сентября2026,05:29МСК: **идёт обучение**, завершено8из168пакетов. Сейчас mixed/patch5m/wide, третий внутренний fold;4 962 566параметров. Скорость около13,1шага/с на пакет из6моделей. Оценка оставшегося обучения примерно8ч08мин; пока финальные пакеты считаются по2048шагам. После выбора настроек прогноз изменится. Независимая итоговая проверка в этот прогноз не включена.

Новая серия проверяет диапазон цветовой поправки: прежний tanh, расширенный4*tanh(z/4) и линейный выход. Архитектуры, число параметров, данные, начальные модели и процедура обучения одинаковы. Прежние tanh-контроли используются из проверенного AS. Все7архитектур сохранены, включая4прохода уточнения и динамическую маску связей. План168новых пакетов/1008траекторий; это не1008различных архитектур.

Основание — выявленный на исходномTRAIN предел выразимости выходной поправки. Это гипотеза о причине части ошибки, а не уже измеренное улучшение точности. Новые спектральные данные не подмешаны в это сравнение: их подготовка к переносу остаётся отдельной работой. Используется прежняя измеренная nativeLab-разметка MSKCC966наблюдений/24человека, с сохранёнными внутренними группами и исторически повторно используемыми исследовательскими ролями. Независимая точность цвета обычного селфи ещё не проверена.

## Выполненные проверки

- AS повторно проверена без изменений: seal`ce2a0f51c2a395aced8b3dad51e736441d13fccdeb9932e721e2f54f0734c2e9`, процесс82124завершился с0.
-16CPU-тестов адаптера/обучения прошли за4,27с. Ещё2теста выбора/путей контрольных моделей — за1,67с;2теста монитора — за0,34с. Проверка стиля новых файлов прошла.
- CUDA-проверка всех7архитектур:84экспорта старого режима совпали побитно сAS на шагах8и16. Оба новых выхода каждого семейства прошли64шага; максимальная ошибка CPU/GPU по синтетическим запросам0,0000017791nativeLab при неизменном допуске0,002иrtol1e-6. Пиковая выделенная память1 310 955 008байт. Это проверка реализации и памяти, не показатель качества на реальных фотографиях. Процесс22236завершился с0; SHAпроверки`b768787f660650ea751dcc311fc3f466bb3149ae82cffb0d9d1e0a4dc1ae2494`.
- Протокол/исходники/родительские артефакты зафиксированы доproduction-обучения: SHA`dfb96f902658009f57d37478ee9e2964c951a2bbce8aaba9acda77ff142652b2`. Процесс фиксации18263завершился с0.
- Независимый NumPy-потребитель проверил первые7полных внутренних пакетов:126моделей на128/512/2048шагах,30 528OOF-векторов, максимум расхождения0,0000065154nativeLab. Проверены строки, разделение людей, исходные warm-модели, нормализация по fit-части и контрольные суммы. CUDA-контекст проверяющего процесса не создавался. Это промежуточная проверка завершённых пакетов; она не заменяет будущий полный аудит серии. SHA`c20fdfd5ec1ea90bc66aa80c087dbdfa612c6862fd1494f107a5f635f0ce7e64`.
- Интерфейс собран и проверен в Edge:desktop1365×900/mobile390×844, обновление по кнопке, реальный прогресс6/168при снимке, скорость13,1шага/с, прогноз8ч15мин. Нет ошибок JavaScript или горизонтального переполнения. Последующий API-снимок показал8/168; картинки являются снимками прежнего момента.

## Текущий процесс и продолжение

Основное обучение: tool-session`73932`, фактическийPID`22100`, родитель`13432`. Команда`python.exe -u scripts/chromaseed_head_range_run.py`; лог`D:/Luma-RnD/chromaseed_head_range_v1/production_v1.log`. Процесс проверен черезWin32_Process, прогресс меняется; наблюдалось100%GPU,3525МиБ,57°C. Второй GPU-опыт одновременно не запускать.

Артефакты находятся в`D:/Luma-RnD/chromaseed_head_range_v1`, связанной junction с`experiments/runs/chromaseed_head_range_v1`. Порядок работы уже находится в запущенном runner:126внутренних пакетов → фиксация384оценок и87выборов →42окончательных пакета →207строк итоговых результатов с прежними контролями. Никакие настройки не выбираются по внешним ошибкам этой новой серии.

После реального завершения primary с кодом0 нужны полный независимый аудит, отдельные замеры стоимости и итоговый отчёт. Штатная остановка наблюдения или отсутствие новых строк лога не означает завершение процесса. Новые источники/протоколы HR теперь заморожены; исправления требуют отдельной версии, а не изменения уже идущего опыта.

Следующая независимая работа, пока GPU занят: подготовить группы спектральной палитры из19измеренных TRAIN-лиц, квалифицировать выбор участков кожи и явно отделить производный RGB-рендер от реальных камер. Это не основание сейчас открывать ранее сохранённые test-части или выдавать синтетический цвет за приборную разметку телефона.

[Протокол HR](chromaseed_head_range_v1_protocol.md) · [Дашборд](http://127.0.0.1:8766/#overview). Общая исследовательская цель остаётся активной.
