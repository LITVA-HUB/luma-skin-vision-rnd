# ChromaSeed-NP: fixed ND prefixes and exact export

2026-09-13. Registered after sealed ND, before NP selection or evaluation. This is conditional engineering on repeatedly exposed original TRAIN scenarios, not fresh validation. No new data, images, weights, packages, agents, publication or legacy validation/calibration/test access.

Earlier R already tested per-input threshold exits with shared weights. NP instead removes unneeded unshared ND blocks with one fixed prefix per role/family/policy. No per-image selector or new learning objective. Preserve the original ND learning rate and checkpoint for each of three roles and five families; do not reopen their search. Three seeds 17/29/43 and the same three person-disjoint inner folds apply.

For each family evaluate all prefixes J=1..K on its saved inner OOF at the selected ND rate/checkpoint. There are 45 candidates, each averaged over three seed errors, and 30 policy choices. Primary score is native DeltaE00 equal-person mean, then image p90. The quality policy minimizes (error, p90, numeric bytes, executed blocks, J). The compact policy admits prefixes with error <= best error + 0.10 DeltaE00 and minimizes (numeric bytes, executed blocks, error, p90, J). The +0.10 threshold is a fixed provisional engineering tolerance, not a validated cosmetic requirement. Save all candidate rows, both choices and original full-prefix controls before any new external evaluation. No selection across families by external results.

Export every final prefix: 135 exports from 45 previously trained ND models, plus 18 unchanged FG/random references =153 records. FP32 normalizers and weights; numeric byte count includes two uint8 metadata scalars original_k and prefix; family strings and ZIP overhead reported separately. The first executed block has no state inputs (remove their three weight rows exactly). For blind4, earlier blocks cannot affect the Jth clean output: export only that head and remove its state weights. For other families store blocks 1..J. Preserve schedule denominator ORIGINAL K, with a[t]=sin(pi*t/(2K)), s[t]=cos(pi*t/(2K)); never reschedule to J. Return clean output c[J-1], not the next latent state. Skip the final unused state update. Full-prefix exports isolate these exact algebraic improvements from stage selection. Plain remains one 36->69->3 block.

Actual deployment consumer accepts one finite color36 vector and returns native Lab3, using FP32 normalization and cached FP64 arithmetic. A separate batch interface exists for research. Verify all 405 inner prefix exports against independent original ND recurrence and saved inner outputs. Independently reconstruct all 153 final predictions across all 33 existing affine transformations, verify exact pruning, metrics and both policies, and invoke the actual single-vector consumer on every transformed row (2,016,234 calls). Maximum absolute native-Lab parity tolerance 2e-8; preserve failures. Report all prefixes, full controls and both policies. Descriptive paired person bootstrap uses 20,000 draws with seed771031, fixed predictions, not selection-adjusted inference.

No new neural fitting in primary. Measure actual one-vector CPU response for all153 exports/references and45 original ND controls in the same run, 20 warmups then3 passes. Runtime array bytes exclude interpreter overhead, latency excludes image decoding/skin extraction. For seed17 and every unique {quality J, compact J, full K} of each role/family, perform3 complete standalone reconstructions using unchanged ND fit at the original full architecture/rate/steps, then export J. First repetition is timing warmup; retain its correctness check. Charge input preparation, samples/noise, all original blocks and optimizer/graph setup/training/export, plus NP export; record ND receipt cost and enclosing end-to-end reconstruction separately. Require parent and prefix arrays bitwise equal; prediction parity <=2e-8. Prefixes do not save original training cost, especially e2e prefixes whose weights received gradients through later blocks. GPU timing is warm process/context, RTX4060, single model rather than bank time divided by slots; no new FG training-time measurement.

The primary code/tests/protocol and all consumed parent artifacts are frozen before selection. Independent audit and timing outputs are sealed with reports. After sealing only read-only verification may run; never rerun ND audit/runtime or old G/GS mains. Ordinary-phone facial accuracy and end-to-end product quality remain unvalidated; the broad goal remains active.
