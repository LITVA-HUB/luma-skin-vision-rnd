# HR recovery v1 — Windows progress-file contention

The initial production process22100/session73932 terminated with exit1 after18complete banks. The traceback is a WinError5 PermissionError at Path.replace(progress.tmp,progress.json), in the progress callback for mixed/soft5m/wide/fold0. The last successfully recorded step is1536; the next128-step callback failed. This is an I/O failure, not an observed optimizer/CUDA/numerical failure. The exact holder of the destination handle at that instant is unknown. A Windows reader without delete-sharing is a candidate mechanism; the regression test holds such a handle against the original writer and verifies both failure and recovery after release.

Preserve the original production log, failed job status, progress and remaining temporary progress file as byte-identical recovery evidence. Verify all18complete bank receipts and their files before resume. No completed model is retrained or replaced. The incomplete bank did not reach the original post-fit save step and must restart from its registered initialization. Its discarded elapsed work remains part of total operational cost.

Do not edit the185sources in the original dfb96f902658009f57d37478ee9e2964c951a2bbce8aaba9acda77ff142652b2 source lock. A new, separately frozen launcher wraps only the runner's imported write_json callable. It retries PermissionError after.01/.02/.04/.08/.16/.32seconds, then ten.5second waits (maximum5.63seconds). It passes the identical path and object to the original serializer. After all retries, only the exact progress.json telemetry write may be skipped with a logged diagnostic. Required source/selection/receipt/result/job writes retain fatal failure. Non-permission errors propagate immediately. The model, loss, gradients, initialization, samples, schedule, checkpoints, selection and evaluation callables are unchanged.

Tests cover transient denial, unchanged payloads, critical-write failures, non-permission failures, and a real Windows sharing-handle reproduction with the original writer. Freeze launcher/test/protocol plus all completed outputs and failure evidence before resuming. The original runner verifies its own full source/input lock and reuses completed banks. This recovery does not change the registered search or add any new training data. Timings from a resumed run need the first attempt and discarded partial bank accounted for separately; a post-resume job timer alone is not the full training cost.
