# ChromaSeed-NB: retained independent blocks, matched construction

2026-09-13. Previous NP continuation was verified progress. This protocol is registered before new NB fitting. Original TRAIN only; no new data/images/weights/packages, agents, publication or legacy validation/calibration/test. NP read-only verification passed f9bd98ddad578678278581ce2fde786ced806e13e1db3a15cea1aa0fd33fe024.

Question: does training only retained independent ND blocks reproduce the same NP model while reducing complete construction time? ND already demonstrated local gradients, per-block AdamW/clipping and independent model slots. NP already prunes inference weights/blocks. Neither tested the actual omitted-block construction here; do not claim independent learning itself is new.

Eligible families: local2,local4,blind4. Use NP's unchanged per-role ND learning rate and the union of {quality prefix, compact prefix, original full prefix}. This gives18 unique role/family/prefix settings, two per each of9 role/family pairs. No new prefix/rate/step selection. Plain and e2e4 are unchanged contextual NP references. E2e4 must NOT use the subset trainer because later heads propagate gradients to earlier heads.

For local families retain original block indices0..J-1. For blind retain only original block J-1. Preserve original K schedule, original block initialization (seed+310003+1009*block), weighted row samples, per-block loss mean over64x3 then sum, clipping norm5, AdamW rate/betas/eps/decay, and ND FP32 normalizers. A local block's inference-zero state is not its training state: first block still trains on noise, with all39 inputs. Keep those training weights, prune only during the existing NP export. Generate the complete original-K noise stream on CPU and then select the required coordinates before GPU transfer; charge that full generation and slicing. No reduced random array shape or generator-stream change.

Primary: for each of9 role/family pairs run one original full ND bank plus two retained-block banks, each with seeds17/29/43 at the unchanged selected rate,8192 steps and checkpoints512/2048/8192. Total27 banks,81 trajectories,243 checkpoint payloads (81 full+162 retained). This deliberately tests equivalence at longer training as well as selected settings; it does not reopen model selection or promote an externally favorable later checkpoint. Same seed/rate gets identical samples/noise/initialization. Save raw retained training weights and original block IDs; export every retained checkpoint via the exact NP payload format. Compare all162 subset checkpoint exports to the matching fresh full control, and the54 selected-checkpoint exports to the immutable NP models. Report bitwise status, maximum weight drift and prediction drift. Intended result is bitwise equality. Fixed engineering acceptance bounds if floating-point grouping differs: weights atol2e-6/rtol2e-6, maximum absolute native Lab0.002, absolute equal-person DeltaE00 change0.002, explicitly labelled approximate if nonzero. Never silently widen bounds or relabel approximate as exact.

Check all162 subset-checkpoint outputs and81 full-checkpoint controls on all33 existing affine transformations of each role's held side. New high-step outputs are parity/stress evidence, not selection evidence. Independently evaluate full original ND recurrence, verify retained arrays/indexing/normalizers, match each exported NP payload and actually call the single-vector NumPy consumer for every subset transformed row. Recompute native DeltaE00/person/site/camera metrics, all transform/dose summaries and selected NP comparisons. Do not report a new accuracy gain when equality is the expected result. Reused original TRAIN scenarios overlap and camera/person composition is confounded; ordinary-phone faces remain unvalidated.

Timing: for each of18 unique settings and seed17, perform3 paired COMPLETE standalone fits of original full network plus prefix export versus retained blocks plus prefix export, at NP's originally selected step. First pair is warmup for timing only; retain its correctness checks. Alternate order full/subset then subset/full then full/subset. Total108 full standalone reconstruction calls. Count preprocessing, weighting/sampling/noise, initialization, GPU transfer, all retained updates, optimizer/graph setup/reset, checkpoint validation and export. Preserve full fitting receipt and enclosing complete timer, never divide bank times by slots. CPU one-vector response uses unchanged NP consumer; confirm parity instead of claiming a new inference-kernel gain. Record GPU allocation with its limited tensor-memory scope; no process-memory claim. Compare same-run paired costs, not prior historical clock conditions. CUDA FP32 deterministic/TF32 off, RTX4060, one CPU thread, warm process/context.

Before primary freeze: synthetic CPU/GPU behavioral tests for original block IDs, noise coordinates, optimizer independence, full/subset/eager/graph equivalence and e2e rejection; lint all new primary sources. Independent audit and timing are separate and sealed afterwards. Failure/approximation counts and full controls must be preserved. After sealing report verifier only; preserve all NP/ND and earlier artifacts, never old G/GS mains. The broad goal remains active until real facial quality is independently established.
