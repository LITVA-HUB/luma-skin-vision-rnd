# NG: inner-only regularization geometry of NR

Registered 2026-09-13 before calculation. The preceding forum turn was progress: it added verified external candidates and ruled out unsuitable training data. NR remains the latest numerical result. Fresh NR read-only verifier57298 exited0 with unchanged receipt3e4079df5d688136ef59be6b599633b0bc7c44d9d21f410eaeefc2aef0699c65. The full compact/fast/high-quality objective remains active. Existing user authorization covers inline local work; no agents or publication.

Question: does NR's preference for alpha10 warrant one bounded stronger-penalty experiment, or merely reflect an already almost constant predictor? Inspect only the nine original inner banks, 378 fixed bases and two objectives =756 designs. No new head, hidden network, prediction selector or outer metric is fitted in NG. Original TRAIN only, hashd7e7b4b4fc4574d620cbac8364dd8bb91be51769f4045bb8b4ddf2ad840556e0; no other dataset endpoints. Frozen NR selections/OOF predictions supply84 curves with252 existing candidate scores, not new independent evidence. W previously improved every internal score yet worsened13/15 external comparisons; stronger regularization here is a different representation-specific hypothesis, not a novelty claim.

Reconstruct the unweighted standardized ReLU design Z=[1,H]. Preserve NR FP32 input-normalization arithmetic and stored FP32 first-layer arrays, followed by FP64 activation arithmetic. Each fit subset supplies its own unweighted hidden mean/std with floor1e-6. Weights retain mean1 person/site/image balancing. Normalized loss uses G=Z'WZ. Perceptual loss uses the coupled195-dimensional Gram with the original finite-difference local CIEDE2000 tensor, normalized by weighted mean trace after target-standard-deviation scaling. The intercept is not penalized: one coordinate per norm output or three coupled coordinates.

Partition G into bias/hidden blocks. Compute S=G_hh-G_hb solve(G_bb,G_bh); eigenvalues of S define effective degrees of freedom. For norm report total3*(1+sum(lambda/(lambda+alpha))); for perceptual total3+sum(lambda/(lambda+alpha)). Equivalent per-output df is total/3, not the number of independent people. Alphas .1,1,10,100,1000 are geometry probes, not selected models. Check finite SPD metrics and tolerate only roundoff-scale negative eigenvalues before clipping them to zero.

Independently reconstruct hidden normalization and the analytic perceptual tensor. Whiten the observation design, QR-project away the intercept columns, then use singular values squared rather than the Gram Schur complement. All756 spectra must agree at atol2e-7*max(1,largest eigenvalue), rtol2e-6; all3780 df values at absolute3e-5. Also independently recompute all252 original inner candidate scores from stored OOF with the established independent DeltaE00 scorer at2e-8. Unit tests compare df with the direct full hat-matrix trace, verify unpenalized-intercept limits and shifts, and reject invalid data.

Report per-role/group/objective/basis geometry, counts of almost-constant columns, spectra, old score gaps, and complete coverage. No outer scores may select the next range. A single .1/1/10/100/1000 readout extension is justified if at least56/84 old curves improve from1 to10 AND the median equivalent per-output df reduction from10 to100 across all756 designs is at least1. Otherwise defer that extension and record which condition failed. Neither outcome proves camera or real-face accuracy. New data/weights/packages are excluded; preserve all parent artifacts.

Primary/independent calculation sources and every consumed NR artifact are hash-bound before calculation. Results, full eigenvalues and runtime are separate outputs. A report/verification receipt seals output hashes and fresh tests after calculation. Mutable plan/goal/status documents are not immutable inputs.
