# NB failed equivalence: complete the diagnostic without relaxing guards

Post-primary addendum,2026-09-13. Primary session12062 is authoritatively terminal exit1; process inspection found no matching worker. All27 training banks and243 raw checkpoint payloads finished. The frozen evaluator stopped in mixed/local2 at8192 on its unchanged2e-6 weight comparison (reported w0 maximum0.03171223). Its result/progress completion files were not produced. Do not rerun/rewrite the primary or interpret the stop as unfinished training.

Read-only inventory of all raw pairs finds19/162 subset checkpoints outside the originally registered weight tolerance;63 are bitwise exact. At512 all54 pass tolerance, at2048 two fail, at8192 seventeen fail. One of54 selected-setting subset checkpoints fails against the matching fresh full bank: reverse/local4/J2/seed43/2048, raw maximum0.0087223463. These observations are post-hoc failure diagnosis, not a revised acceptance rule. GPU shape/reduction rounding is a hypothesis to test, not an established cause.

Complete all originally intended243 outputs/33 transforms into separate diagnostic model/prediction folders and diagnostic_results.json. Preserve the primary's partial exports. Record each original weight/prediction/error guard as a boolean and publish failures; do not assert that the failed optimization is equivalent. Independently audit all raw arrays, block identities, normalizers, exact exports, native-Lab recurrence, metrics and actual consumers. Complete the originally planned108 paired selected-setting reconstruction timings, keeping failed correctness flags beside times; a faster incorrect model is not an accepted optimization. Keep original NP/ND models as the accepted fallback.

Add18 single-step causal-isolation probes (9 role/family settings x2 prefixes) with the same three seeds, exact initial weights, row samples and original noise: compare outputs, gradients and AdamW updates. Also inject the full model's identical sliced gradient into a fresh retained optimizer to separate forward/backward differences from optimizer arithmetic/grouping differences. These are diagnostic probes, no selector or replacement model. Use original TRAIN fit side only. Preserve original source lock and all tolerances. The original strict equivalence experiment remains failed even if selected singletons happen to pass.

After diagnostics and timing, seal an honest negative/mixed report; report verifier read-only after sealing. The broad goal remains active. No new data/images/weights/packages, agents, messages or publication.
