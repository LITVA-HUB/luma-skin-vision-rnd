# After NS: stop expanding this penalty grid; investigate a different learning target

NS is verified numerical progress, not broad-goal completion. NG correctly identified remaining effective capacity and motivated one bounded extension. The extension tested5040 primary/independent-QR heads on504 exact NR bases, preserving3540 old model controls. Stronger alpha100 is selected in10/84 settings;67 keep10,7 keep1,none selects1000. Five changed heads improve and five worsen versus old NR,74 are unchanged. All12 frozen policies still lose to FG. Grid expansion has answered its question; do not continue adding penalties or select favorable external exceptions.

Mixed raw36 norm policy worsens5.502520→5.678073; perceptual policy changes to Adam16/alpha100 and improves5.654729→5.607624, but FG remains5.438652. Reverse raw36 norm improves10.067767→9.539056 yet remains worse than FG8.705018. Two policies improve versus old NR, one worsens, nine stay unchanged. These are repeated role estimates; paired fixed-prediction intervals are descriptive and do not correct model-selection uncertainty. Full construction remains much more expensive for learned representations than random features or FG.

The next different question is block-local denoising/refinement inspired by NoProp. Before implementing, inventory the existing R iterative residual learner, G/H conditional corrections and prior frozen/palette heads. NoProp trains blocks on deliberately noised target states, then denoises at inference; our R sequence already performs repeated corrections but uses a different training target. A continuous-Lab adaptation is a hypothesis, not a reproduction of the original classification paper or a novel invention.

Register a small fixed two/four-step comparison with an ordinary matched total-parameter regressor and the existing analytical references. Define training-only noise schedules/targets, inference initialization and whether blocks share weights before fitting. Avoid leaking the true Lab into inference or choosing stopping thresholds from outer errors. Charge every block/iteration and representation construction in size and timing; run CPU smoke checks before any RTX4060 batch, with no concurrent heavy benchmark jobs. Selection remains inside original TRAIN person folds. Quantify whether repeated corrections improve real target error rather than merely converge or flatten predictions. This is planned, not implemented or launched in NS.

Only original TRAIN under the current boundary: no agents, new data/images/weights/packages, publication or exposed legacy validation/test. The forum supplement records NIST/ENCoDE leads but no data acquisition occurred. Real ordinary-phone face/end-to-end quality remains unvalidated. The full compact/fast/high-quality goal remains active, and this learning-target investigation is a meaningful available next action.

[NS report](../benchmarks/chromaseed_neural_shrinkage_v1/report.md) · [NG geometry](../benchmarks/chromaseed_neural_geometry_v1/report.md) · [Forum leads](chromaseed_forum_followup_after_nr_2026-09-13.md) · [Earlier iterative R](chromaseed_refine_next_decision.md).
