# WE: earlier and lower-rate training of quality-oriented ChromaSeed models

2026-09-13. Previous goal turn is VERIFIED PROGRESS: WIDE source and artifact receipt d86795ebc9659fdefe869298fa8a44106555e5ac90a5fa153105d408be3b4876 freshly passed read-only verification. Its primary/audit/runtime are terminal. User's latest priority is maximum skin-color quality; small size is secondary. This study tests the observed search boundary:11/12 larger WIDE choices selected earliest512, and all15 capacity choices selected lower rate0.0001. No first-use or universal quality claim.

## Fixed scope before code or fitting

Architectures m31/m61/m111/m832 are EXACT unchanged WIDE models, with30915/60611/110979/832259 parameters and unchanged NumPy consumer. Tiny and NP remain frozen comparison controls. Only original TRAIN966rows24people, cache d7e7b4b4fc4574d620cbac8364dd8bb91be51769f4045bb8b4ddf2ad840556e0; color/tokens/native Lab and grouping fields only. No new images/data/weights/packages/agents/publication and no legacy validation/calibration/test arrays. Reused roles remain exploratory; no new ordinary-phone facial benchmark.

Matching NP warm parents, fit-only normalizers, original seed17/29/43 streams, loss, clipping, AdamW, FP32 deterministic CUDA and entire six-slot layout remain WIDE's. The cosine horizon remains8192 even for short fitting. Change only the first rate in each seed's pair: (0.00001,0.001), (0.00003,0.001), or original (0.0001,0.001). Each pair is its own six-slot bank ordered seed then first/control rate. The high-rate slots are deliberate identical canaries, not extra candidates or independent runs for averaging.

All108 inner banks (4 capacities x3 rate pairs x3 roles x3 folds) train through2048 with checkpoints0/32/128/512/2048.648 trajectories and3240 checkpoint payloads, including648 zero-step aliases. For the original pair, all six payloads at512 and2048 must be bitwise equal to WIDE (432 matched positive payloads). For the two lower pairs, all three high-rate canaries at every checkpoint must equal the canonical pair, within the same fold/capacity/seed (1080 canary payloads including216 zero-step aliases). Freeze code before fitting; numerical guards do not change after failures. Short-budget results do not establish optimality at lower rates; record a new upper-bound preference honestly if it occurs.

## Candidate space and final evaluation

Per role/capacity: each lower rate has four positive steps32/128/512/2048; each original rate0.0001/0.001 has the same four plus its existing WIDE8192 checkpoint.18 positive candidates/capacity; common NP baseline and old inner-selected tiny control add two candidates per role.222 scores total. Average three equal-person native D65/10-degree DeltaE00 scores, not ensemble predictions. Choose by(error,p90,numeric bytes,step,rate), one positive configuration per larger capacity plus one overall choice per role:15 choices. Old WIDE per-capacity selected settings remain in the expanded candidate space; independently reproduce their scores and keep their exports as controls. The tiny control's grid is not expanded, so this is an optimization-schedule study, not a fresh fully matched architecture comparison.

Freeze all choices before final fitting/evaluation. For a chosen old rate at step512/2048/8192, import the exact WIDE final checkpoint. Otherwise train one full six-slot final bank for that role/capacity to the selected step, original horizon8192; for chosen high rate use canonical first rate0.0001. Zero to12 new final banks, actual count reported. All final warm parents remain matching full-fit NP. Retain36 new selected exports,36 old WIDE per-capacity selected controls,9 NP and9 old selected tiny controls:90 records, with unchanged-setting aliases labelled explicitly. No replacing frozen choices with favorable observed external checkpoints.

Each final record is evaluated under the same33 joint color/token affine settings: identity and eight RGB corners at1/255,4/255,16/255,64/255.1,186,020 prediction vectors including identity. Synthetic target invariance is a diagnostic assumption. Store all inner learning curves and negative candidate outcomes; final evaluation compares preselected new settings against old controls, not every unselected new setting.

## Verification, timing and continuation

Before freeze, test new-fit identity at original rates, fixed-shape/horizon short replay, unchanged high-rate canaries despite changing the paired neighbor, and actual lower-rate application without mutating immutable baselines. Compare CPU/CUDA behavior on synthetic arrays; no broader generalization inferred. Reuse the independently audited WIDE consumer and non-BLAS contraction reference unchanged. Audit all inner exports/OOF rows, original checkpoint equality/canaries, normalization and sampling, all222 scores/15 choices, every final export and actual single-example consumers for every stress row. Native Lab tolerance2e-8, CUDA/native.002, exact payload guards unchanged.

Time all90 final consumers with20 warmups and three passes on one CPU thread, including patch normalization and encoding, excluding image decoding/extraction. Reconstruct all12 new selected capacity recipes three times: include three original ND singleton trainings, NP exports, and the full six-slot continuation through the selected step. Original8192 horizon and rate pair must remain exact. Verify every chosen export and all six reconstructed final-bank payloads against the corresponding source; charge full construction rather than dividing by slots. WIDE's former construction timings are historical references, not fresh paired measurements. Record actual research trajectories, presentations and setup/export costs, including duplicate canary work.

Seal verification/report/card/decision and reverify read-only. Preserve every older sealed writer, especially WIDE/WA/LT/NP/ND and G/GS. P8 primary remains complete but its audit/runtime are explicitly deferred by user quality steering. Keep the full goal active unless actual ordinary-phone/end-to-end quality requirements become true and verified; software checks cannot prove that broader outcome.
