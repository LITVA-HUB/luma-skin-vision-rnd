# Source allocation-transfer provenance

Original MSKCC CC-BY TRAIN and source VALIDATION images/native instrument Lab
are reused. No new external dataset, weights, copied author implementation,
paid resources or participant publication. Probability construction uses only
selected TRAIN targets/person/site metadata. Camera labels define evaluation
protocols; they are not inputs to the sampler or deployed model.

Prefit tag:checkpoint/skin-sampling-transfer-pretrain-2026-09-11. Exact-byte
source lock, probability/draw/state hashes,90-array audit and final report are
preserved. Private images, identifiers, checkpoints and NPZ files remain out
of Git. No exposed independent TEST/CAL access.

Target balancing, hierarchical sampling and importance correction are established.
The measured combination fails transfer controls, so it does not establish an
invention or general technical advantage. A planned privileged-reference offset
diagnostic must never be marketed as our single-image uncalibrated technology.
