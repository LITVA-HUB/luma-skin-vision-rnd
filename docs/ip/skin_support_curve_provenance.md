# Support / representation experiment provenance

Original MSKCC CC-BY images and actual native instrument Lab are reused only via
the existing verified TRAIN cache. No new dataset or external model weights were
adopted. No participant images, identifiers or checkpoints are committed.

Prefit checkpoint: checkpoint/skin-support-curve-pretrain-2026-09-11. Exact-byte
source lock, private output digests, audit and report bindings are retained.
Post-fit interventions are explicitly separate from the primary fixed model.

Residual adapters, convolutional patch encoding, learning curves and sampling
controls are established techniques. No invention is claimed from this partial
or negative result. Fresh primary-paper review reinforces that supervised skin
Lab prediction is prior art; article availability does not clear external weights
or their pretraining datasets. Nothing was imported from those author models.
