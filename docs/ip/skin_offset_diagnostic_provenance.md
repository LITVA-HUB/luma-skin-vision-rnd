# Privileged-reference offset diagnostic provenance

Existing MSKCC CC-BY source arrays and actual native instrument Lab only. No new
data, model weights, neural fitting, paid resources or participant publication.
Prefit diagnostic tag:checkpoint/skin-offset-diagnostic-pretest-2026-09-11.
Input/output hashes and exact-byte protocol/source bindings are retained; private
participant identifiers and prediction/reference arrays are not committed.

Offsets use OTHER source evaluation people's instrument references. This is a
privileged calibration comparator, never a single-image or new independent-test
claim. The post-run squared-Euclidean residual identity is separately labeled
and does not replace primary CIEDE2000. No novelty/patentability claim follows.

Fresh gradient/domain-generalization paper review is conceptual prior art only;
no external implementation or weights were adopted. Any future implementation
must preserve this distinction and the existing no-calibration inference goal.
