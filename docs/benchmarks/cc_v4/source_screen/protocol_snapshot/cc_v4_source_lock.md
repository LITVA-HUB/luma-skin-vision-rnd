# V4 source development screen lock

Frozen before any V4 real training, 2026-09-10. Specification cc_v4_spec.md. No test/INTEL GT decoding. Original train1126/validation119 capture groups and V3 cache hashes unchanged. This validation set has been used repeatedly; every result is development evidence.

Run transport, generic action-conditioned router, and action-independent posterior, seed17,120epochs,batch32. A2epoch transport integration pilot is separately archived; it only checks finite training/evaluation, not feasibility of the cost head (20epoch warmup). All full runs start identically from scratch. Direct point prediction is measured within each run, and its own best epoch/checkpoint retained. A separate bit-identical training of mode=direct would duplicate the posterior run, so direct is a policy/readout control. Historical strong direct C+ remains in the report; this screen is not the final calibrated matched C+ comparison.

Same loss: point reproduction degrees + w(epoch)*[.05*mean squared angular-risk error in degrees +25*mean squared sin²-risk error]. w=0 through20, linear to1 at40, then1. No derivative term in first two runs. AdamW .001,wd.0001,cosinefloor.00002,clip5,noAMP/TF32. Standard horizontal flip and common exposure only. Action sampler: detachedpoint,16local uniform±.4,16global uniform[-1.5,1.5];clamp[-2,2]. No GT-centered sampling. Batch order/augmentation RNG is the same between matched runs.

Best searched epoch minimizes valid step2 reproduction mean. Preserve separately best direct-point epoch. Evaluate1/2/4 refinements from one cached image encoding:25/51/103 candidate queries. Every step retains previous center; lower model-predicted risk is not proof of lower actual error. Report per-image worsening, true candidate oracle and risk at fixed coverage, explicitly uncalibrated.

Source filenames/GT/reference metadata do not enter the neural model. New INTEL collection remains opaque to numeric fitting/evaluation. No pretrained teacher assets. Data rights: SimpleCube++ CC BY4.0 under already recorded original terms; this screen trains only on that source.

Preflight revision before training: linear log transport can be absorbed into the MLP first layer. Replace it with explicit nonlinear corrected-simplex means/RMS and add the action-mode null using simplex at the initial point plus relative action. The input size and module graph remain matched. This tests physical inductive bias, not new information or expressivity. Design review is preserved. Runner marks other budgets/gradient settings as is_frozen_primary=false, including the declared pilot. Final artifact_manifest hashes every run file. Latency is measured in a separate fresh-process benchmark after fitting; no model timing claim is made from training duration.

Next tests require evidence: derivative loss and physical intervention/EMA ablations are planned separately, not hidden changes to this run. Equal-query nonadaptive search is required before attributing an advantage specifically to iterative refinement. If the transported decision field loses to the posterior or generic action router, record it and prefer the simpler winner pending further evidence. No major export optimization before positive evidence.
