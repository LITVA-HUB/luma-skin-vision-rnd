# Neural shrinkage implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans inline. No delegation is authorized.

**Goal:** Test the registered stronger-penalty question on exactly the same compact representations, including full learning cost and adverse results.

**Architecture:** Import NR bases, use its unchanged primary Cholesky and independent augmented-QR heads, then preserve original inner selection and final stress protocol. Separate uncached construction timings from the cached sweep.

**Tech Stack:** Existing explicit Python/NumPy/SciPy, one CPU thread, pytest/ruff. No installation.

**Spec:** docs/research/chromaseed_neural_shrinkage_v1_protocol.md.

## Global constraints

- Original TRAIN only and all parent numerical sources/receipts immutable.
-504 bases,5040 heads,5556 records;3540 exact old controls;420 scores/84 choices/12 policies;381 final models/5020818 prediction rows.
- No outer-based selector repair. Full timing270 reconstruction fits; disclose all representation cost.
- Keep the full goal active unless independent real-phone quality is actually established.

## Task1: exact-base training and frozen selection

Create scripts/chromaseed_neural_shrinkage_train.py. Reuse tested fit_head/refit/name_for/choose/choose_policy; no new numerical solver is introduced.

- [x] Implement source freeze and12 role/fold banks, import exact NR bases and all43 reference payloads per bank.
- [x] Fit five alphas, independently reconstruct each head, verify old controls/OOF, save both primary/QR banks and receipts.
- [x] Reassemble OOF and select before final fitting; reproduce every old candidate/reference score.
- [x] Evaluate all381 selected cases with33 transforms and persist full results. Poll original live handle to terminal.

## Task2: saved-artifact audit and dedicated runtime

Create scripts/chromaseed_neural_shrinkage_audit.py and scripts/chromaseed_neural_shrinkage_runtime.py. Audit consumes only fixed settings/saved predictions, independent metric/consumer helpers and original TRAIN. Runtime reuses the existing complete timing_fit function, whose dependency hash is locked.

- [x] Check all source/input/bank/model hashes and every actual consumer output; independently recompute all clean/stress scores and compare QR outputs.
- [x] Profile381 actual consumers and270 full reconstruction fits, counting exact exports and the costs of representation training.

## Task3: complete comparison and verification

Create report/CSV and verification under docs/benchmarks/chromaseed_neural_shrinkage_v1; new postprocess script owns generation/read-only re-verification.

- [x] Include all84 aggregated head comparisons and12 policies against both old NR and FG, all four stress doses, sizes and complete learning times.
- [x] Run existing24 NR numerical tests plus9 NG invariants; check ruff only changed files and seal sources/results/runtime/report hashes.
- [x] Update mutable current status/goal/AGENTS and preserve negative results. New next action follows all evidence, not the most favorable role.

No commits, package changes or publication. Existing autonomous authorization covers this bounded experiment.

## Execution evidence

NS completed:primaryPID30112/session54705 exit0, corrected audit82044 exit0, runtime41219 exit0. First auditor unpacking error was diagnosed against the frozen parent interface; full rerun passed with unchanged primary/models/tolerances.33 tests pass;270 full timing exports exact. All12 policies lose FG, so next decision ends this grid expansion. Receipte725b4a6e86df7a51dd9825089ca39c218100bc3e31f765a585946353fb9cd6f; read-only replay passed. No worker remains; broad goal active.
