# ChromaSeed-KF implementation plan

User autonomy is already authorized. Previous K turn is verified progress; no old process is live. Preserve every old source lock. No delegation.

1. Test uniform prefix pair sampling and streaming weighted Cholesky/Nyström against independent and frozen dense algebra.
2. Implement on-demand columns, sampled width and explicit memory/entry accounting. Synthetic-only preflight and edge cases before source freeze.
3. Implement405-readout banks, person-disjoint OOF selection, fast-arm/size aliases, frozen45+9+3 choices, final banks and135 selected model evaluations.
4. Freeze protocol/source/cache bindings, run the registered full original-TRAIN-only experiment sequentially.
5. Independently audit normalization/selection/columns/dense refits and every final prediction/metric; then measure standalone fit, batch-one inference and synthetic allocation scaling.
6. Document measured quality/latency/fit/memory tradeoffs with all controls, update the active-goal ledger, keep the broad objective active unless actual completion becomes verified.

All steps completed2026-09-13. The sampling/size policies did not reliably preserve outer quality. A separately registered exact-condensed follow-up reproduced108 models exactly and reduced128-center standalone training4.50x. See `docs/research/chromaseed_condensed_exact_v1_protocol.md` and `docs/benchmarks/chromaseed_fast_kernel_v1/report.md`. Both runs are terminal,30 numerical tests pass, broad goal active. Next loss-function/refinement study is planned, not launched.
